
User wants geolocation on the Monitoreo map: ask permission, show user's real-time location with a marker that updates as they move. This is totally doable with the browser Geolocation API + Leaflet — no extra dependencies, no API key, no backend.

Plan is small and concrete. Keep it short.
